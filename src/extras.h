#ifndef EXTRA
#define EXTRA
#include <vector>
#include <string>
#include <string.h>
#include <algorithm>
#include <iostream>

// Função de comparação sobre a capacidade das salas.
// Retorna se a sala A tem menos capacidade que a sala B.

bool comparar(const Sala &a, const int b);

#endif